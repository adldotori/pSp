version: '3.4'

services:
  psp:
    container_name: psp
    networks: ['mindlogic-network']
    build:
        context: .
        dockerfile: Dockerfile
    volumes:
     - './:/app/'
     - '../images1024x1024/:/data/'
     
networks: {mindlogic-network: {}}