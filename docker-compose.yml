version: '3.4'

services:
  psp:
    container_name: psp
    networks: ['mindlogic-network']
    build:
        context: .
        dockerfile: Dockerfile
    volumes:
     - './:/app/'
     - '../dataset/:/data/'
     
networks: {mindlogic-network: {}}